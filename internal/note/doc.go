// Package note owns repository-scoped backlog note artifacts and their
// lifecycle. Stores are explicitly rooted beneath a registered repository;
// this package never discovers or reads the retired global note store.
package note
