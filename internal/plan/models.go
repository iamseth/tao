package plan

// Model definitions are grouped by artifact, lifecycle, and summary concerns in
// sibling *_models.go files to keep the plan package navigable while preserving
// a single in-package API surface.
