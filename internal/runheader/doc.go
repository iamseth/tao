// Package runheader renders the live tao run header. It makes metadata control
// characters visible so the fixed terminal layout cannot be altered by state.
// Rendering is presentation-only and never affects run outcomes or supplies
// lifecycle evidence.
package runheader
