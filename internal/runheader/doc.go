// Package runheader owns the live tao run header view model and renderer. It
// makes metadata control characters visible so the fixed terminal layout cannot
// be altered by state.
// Rendering is presentation-only and never affects run outcomes or supplies
// lifecycle evidence.
package runheader
